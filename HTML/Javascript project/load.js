const doc = './Goldie Home.html';
function Scramble(data)
{
const options = { test : false || {data} || {} };
    options.test = options.test || {data: data};
return (doc.indexOf('./')) ? doc.substring(null,doc.indexOf('.')) : doc.substring(null);   
}

console.log(Scramble(12));
